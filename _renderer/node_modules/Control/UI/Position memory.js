module.exports = class PositionMemory extends WebComponentAbstract {
  init() {
    
    var p = this.new('p')
    p.new('a', true, { textContent: 'Save current coordinates', className: 'button' }).on('click', e => this.send('G60'))
    
    var p = this.new('p')
    p.newText('Restore saved coordinates')
    p.new('br')
    p.new('a', true, { textContent: 'X', className: 'button' }).on('click', e => this.send('G61 X'))
    p.newText(' ')
    p.new('a', true, { textContent: 'Y', className: 'button' }).on('click', e => this.send('G61 Y'))
    p.newText(' ')
    p.new('a', true, { textContent: 'Z', className: 'button' }).on('click', e => this.send('G61 Z'))
    p.newText(' ')
    p.new('a', true, { textContent: 'All', className: 'button' }).on('click', e => this.send('G61 XYZ'))
  }
  
  send(val) {
    self.emit('serialWrite', { data: val, prepend: true })
  }
}
