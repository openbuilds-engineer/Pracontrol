module.exports = class PositionMemory extends WebComponentAbstract {
  initCallback() {
    
    var p = this.newElem('p')
    p.newElem('a', true, { textContent: 'Save current coordinates', className: 'button' }).on('click', e => this.send('G60'))
    
    var p = this.newElem('p')
    p.newText('Restore saved coordinates')
    p.newElem('br')
    p.newElem('a', true, { textContent: 'X', className: 'button' }).on('click', e => this.send('G61 X'))
    p.newText(' ')
    p.newElem('a', true, { textContent: 'Y', className: 'button' }).on('click', e => this.send('G61 Y'))
    p.newText(' ')
    p.newElem('a', true, { textContent: 'Z', className: 'button' }).on('click', e => this.send('G61 Z'))
    p.newText(' ')
    p.newElem('a', true, { textContent: 'All', className: 'button' }).on('click', e => this.send('G61 XYZ'))
  }
  
  send(val) {
    AppEvent('serialWrite', { data: val, prepend: true })
  }
}
