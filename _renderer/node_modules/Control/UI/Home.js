module.exports = class Home extends WebComponentAbstract {
  init() {
    
    var p = this.new('p', true, { textContent: 'Home ' })
    
    p.new('a', true, { textContent: 'X', className: 'button' }).on('click', e => this.home('X'))
    p.newText(' ')
    p.new('a', true, { textContent: 'Y', className: 'button' }).on('click', e => this.home('Y'))
    p.newText(' ')
    p.new('a', true, { textContent: 'Z', className: 'button' }).on('click', e => this.home('Z'))
  }
  
  home(val) {
    self.emit('serialWrite', { data: 'G28 ' + val, prepend: true })
  }
}
