module.exports = class Power extends WebComponentAbstract {
  init() {
  
    var p = this.new('p', true, { textContent: 'Power ' })
    p.new('a', true, { textContent: 'On', className: 'button' }).on('click', e => this.send('M80'))
    p.newText(' ')
    p.new('a', true, { textContent: 'Off', className: 'button' }).on('click', e => this.send('M81'))
    
    p.new('br')
    p.newText('Motors ')
    p.new('a', true, { textContent: 'On', className: 'button' }).on('click', e => this.send('M17'))
    p.newText(' ')
    p.new('a', true, { textContent: 'Off', className: 'button' }).on('click', e => this.send('M18'))
  }

  send(val) {
    self.emit('serialWrite', { data: val, prepend: true })
  }
}
