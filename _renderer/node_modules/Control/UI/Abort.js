module.exports = class Abort extends WebComponentAbstract {
  initCallback() {
  
    var p = this.newElem('p')
    p.newElem('a', true, { textContent: 'Emergency stop', className: 'button' }).on('click', e => this.send('M112'))
    p.newElem('br')
    p.newElem('a', true, { textContent: 'Abort planned moves', className: 'button' }).on('click', e => this.send('M410'))
  }

  send(val) {
    AppEvent('serialDirectWrite', { data: val + '\n' })
  }
}
