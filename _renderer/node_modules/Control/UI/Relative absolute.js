module.exports = class RelativeAbsolute extends WebComponentAbstract {
  init() {
    
    var p = this.new('p', true, { textContent: 'Coordinates ' })
    p.new('a', true, { textContent: 'Absolute', className: 'button' }).on('click', e => this.send('G90'))
    p.newText(' ')
    p.new('a', true, { textContent: 'Relative', className: 'button' }).on('click', e => this.send('G91'))
    
    var p = this.new('p', true, { textContent: 'Extruder ' })
    p.new('a', true, { textContent: 'Absolute', className: 'button' }).on('click', e => this.send('M82'))
    p.newText(' ')
    p.new('a', true, { textContent: 'Relative', className: 'button' }).on('click', e => this.send('M83'))
  }
  
  send(val) {
    AppEvent('serialWrite', { data: val, prepend: true })
  }
}
