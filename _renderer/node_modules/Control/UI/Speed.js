var style = `
::content label { white-space: nowrap; }
::content input[type='number'] { width: 5rem; }
`

module.exports = class Speed extends WebComponentAbstract {
  init() {
    this.renderCss(style)
    
    var AxisSettings = require('./AxisSettings')
    
    this.new(AxisSettings, true, { opt: { name: 'Max acceleration by axis', gCode: 'M201' } })
    this.new(AxisSettings, true, { opt: { name: 'Max feedrate', gCode: 'M203' } })
    this.new(AxisSettings, true, { opt: { name: 'Axis steps per unit', gCode: 'M92' } })
  }
}
