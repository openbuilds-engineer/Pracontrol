var style = `
::content label { white-space: nowrap; }
::content input[type='number'] { width: 5rem; }
`

module.exports = class Speed extends WebComponentAbstract {
  init() {
    this.renderCss(style)
    
    var AxisSettings = require('./AxisSettings')
    
    this.new(AxisSettings, { opt: { name: 'Max acceleration by axis <small>mm/sec^2</small>', gCode: 'M201' } })
    this.new(AxisSettings, { opt: { name: 'Max feedrate <small>mm/sec</small>', gCode: 'M203' } })
    this.new(AxisSettings, { opt: { name: 'Axis steps per unit', gCode: 'M92' } })
  }
}
