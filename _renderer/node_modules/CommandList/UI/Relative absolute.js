'use strict'

module.exports = class RelativeAbsolute extends WebComponentAbstract {
  initCallback() {
    
    var p = this.newElem('p', true, { textContent: 'Coordinates ' })
    p.newElem('a', true, { textContent: 'Absolute', className: 'button' }).on('click', e => this.send('G90'))
    p.newText(' ')
    p.newElem('a', true, { textContent: 'Relative', className: 'button' }).on('click', e => this.send('G91'))
    
    var p = this.newElem('p', true, { textContent: 'Extruder ' })
    p.newElem('a', true, { textContent: 'Absolute', className: 'button' }).on('click', e => this.send('M82'))
    p.newText(' ')
    p.newElem('a', true, { textContent: 'Relative', className: 'button' }).on('click', e => this.send('M83'))
  }
  
  send(val) {
    AppEvent('serialWrite', { data: val, prepend: true })
  }
}
